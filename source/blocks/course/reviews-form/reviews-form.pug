mixin reviews-form
  form(class!=attributes.class).reviews-form
    +input('Имя')(name='name' type='text' placeholder='Имя' required='true').reviews-form__name
    +input('Заголовок отзыва')(
      name='title'
      type='text'
      placeholder='Заголовок отзыва'
      required='true'
    ).reviews-form__title
    +rating(required='true').reviews-from__rating
    +input('Оставте отзыв')(
      name='review'
      type='textarea'
      placeholder='Текст отзыва'
      spellcheck='true'
      required='true'
    ).reviews-form__message
    footer.reviews-form__footer
      +button('Отправить').reviews-form__button
      p.reviews-from__annotation * Перед публикацией отзыв пройдет предварительную модерацию и проверку
