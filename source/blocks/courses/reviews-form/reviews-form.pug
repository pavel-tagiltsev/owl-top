mixin reviews-form
  form(class!='reviews-form' + '')&attributes(attributes)
    +input-group('Ваше имя')(
      name='name'
      type='text'
      placeholder='Имя'
    ).reviews-form__name

    +input-group('Заголовок отзыва')(
      name='title'
      type='text'
      placeholder='Заголовок отзыва'
    ).reviews-form__title

    .reviews-form__rating
      p(aria-hidden='').reviews-form__rating-label Оценка:
      +rating(required='required')

    +input-group('Ваш отзыв')(
      name='review'
      placeholder='Текст отзыва'
    ).reviews-form__message

    footer.reviews-form__footer
      +button('Отправить')(type='submit').reviews-form__button
      p.reviews-form__annotation
        | * Перед публикацией отзыв пройдет предварительную модерацию и проверку
